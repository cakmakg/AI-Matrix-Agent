# Dependencies
node_modules/
package-lock.json

# Test / scratch files
emlakci-test.mjs
*.test.mjs
# NOTE: project test suite lives in server/tests/*.test.js — do NOT ignore *.test.js
# (node_modules/ is already ignored above, so its tests stay out either way).

# Local-only: design prototype + working planning docs (not shipped to the repo)
frontend/agent-matrix-claude/
frontend/docs/NEW_DASHBOARD_ROADMAP.md
frontend/docs/PROJE_DURUM_RAPORU.md

# Environment
.env
.env.local
.env.*.local

# Build outputs
dist/
build/
.next/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Documentation — only the root README is tracked on GitHub.
# Plans, analyses, test notes and per-package READMEs stay local.
*.md
!/README.md
!.claude/skills/**/SKILL.md
